syntax = "proto3";

option go_package="./getfeature";

option cc_generic_services = true;

service GrpcService {
    rpc getFeature (Request) returns (Response) {}
}

message Request {
    bytes image = 1;
}

message Response {
    message R1 {
        message R0 {
            repeated float a = 1;
        }
        repeated R0 b = 1;
    }
    repeated R1 c = 1;
}